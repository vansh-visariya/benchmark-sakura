{
  "id": "refactor_flatten_nested",
  "category": "refactor",
  "tags": ["all", "refactor", "medium"],
  "prompt": "Refactor the function `collect_names` to replace the nested loop with a list comprehension, keeping identical output.\n\n```python\ndef collect_names(people):\n    names = []\n    for group in people:\n        for person in group:\n            names.append(person['name'])\n    return names\n```\n\nExpected: collect_names([[{ 'name': 'A' }, { 'name': 'B' }], [{ 'name': 'C' }]]) == ['A', 'B', 'C']",
  "reference": "def collect_names(people):\n    return [person['name'] for group in people for person in group]\n",
  "tests": {
    "test_basic": "assert collect_names([[{'name': 'A'}, {'name': 'B'}], [{'name': 'C'}]]) == ['A', 'B', 'C']\n",
    "test_empty": "assert collect_names([]) == []\n",
    "test_empty_groups": "assert collect_names([[], []]) == []\n"
  }
}
